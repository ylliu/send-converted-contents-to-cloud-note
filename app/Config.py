class Config(object):
    def __init__(self, appid, secret_key, token, namespace, format, file_path, save_type):
        self.appid = appid
        self.secret_key = secret_key
        self.token = token
        self.namespace = namespace
        self.format = format
        self.file_path = file_path
        self.save_type = save_type
